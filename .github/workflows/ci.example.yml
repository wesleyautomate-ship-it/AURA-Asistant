# Example CI workflow template (copy to ci.yml when ready)
# name: ci
#
# on:
#   push:
#     branches: [main]
#   pull_request:
#
# permissions:
#   contents: read
#   id-token: write
#
# jobs:
#   backend:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'
#       - run: |
#           python -m pip install --upgrade pip
#           pip install -r backend/requirements.txt
#           pip install black ruff mypy pytest pytest-cov pip-audit
#       - run: ruff check backend/app
#       - run: black --check backend/app
#       - run: mypy backend/app
#       - run: |
#           PYTHONPATH=backend pytest backend/tests --cov=backend.app --cov-report=xml
#       - run: pip-audit -r backend/requirements.txt
#       - uses: actions/upload-artifact@v4
#         with:
#           name: backend-coverage
#           path: coverage.xml
#
#   frontend:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'
#           cache-dependency-path: aura-client/package-lock.json
#       - working-directory: aura-client
#         run: npm ci
#       - working-directory: aura-client
#         run: npm run lint
#       - working-directory: aura-client
#         run: npm run typecheck
#       - working-directory: aura-client
#         run: npm run build
#       - working-directory: aura-client
#         run: npm audit --omit=dev
#       - uses: actions/upload-artifact@v4
#         with:
#           name: aura-client-dist
#           path: aura-client/dist
#
#   summary:
#     runs-on: ubuntu-latest
#     needs:
#       - backend
#       - frontend
#     steps:
#       - run: |
#           echo "Backend: ${{ needs.backend.result }}"
#           echo "Frontend: ${{ needs.frontend.result }}"
