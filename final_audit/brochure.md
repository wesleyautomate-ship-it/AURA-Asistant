## Property Brochure Flow — Audit\n\n### Current Status\n- Database schema aligned: BrochureDraft, BrochureTemplate, Property, and PropertyPhoto all exist in dev/test fixtures.\n- dev_data/load.py seeds canonical listing (Marina Heights) plus template and demo agent.\n- /api/v1/templates works with and without trailing slash; /api/v1/brochures can create drafts referencing seeded property/template.\n- Auth logging (audit log timestamps) no longer fails on login, enabling brochure routes to use standard auth flow.\n- Front-end can consume the structured data returned by the endpoints (same shape as mocked store).\n- Tests: pp/tests/test_brochure_routes.py, 	est_brochure_generation.py, and 	est_brochure_simple.py are green.\n\n### Remaining Considerations\n- PDF rendering still depends on WeasyPrint; dev fallback is not yet implemented.\n- AI content still bypasses Gemini by returning deterministic structured copy when API key is missing.\n- RBAC: ensure dev bypass user keeps oles=['agent'] to mirror production permissions.\n\n### Verification\n`ash\npython dev_data/load.py\npython -m pytest app/tests/test_brochure_routes.py\npython -m pytest test_brochure_generation.py test_brochure_simple.py\n`\n
